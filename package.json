{
  "name": "slot-master",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin for bulk work with casino and slot-game cards",
  "scripts": {
    "check": "node --check code.js && node scripts/validate.mjs",
    "test": "node --check code.js && node scripts/validate.mjs"
  }
}
